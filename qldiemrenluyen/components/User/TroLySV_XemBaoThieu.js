import { View, Text } from "react-native";

const TroLySV_XemBaoThieu = () => {
    return (
        <View style={{ padding: 20 }}>
            <Text style={{ fontSize: 18, fontWeight: "bold" }}>Danh sách báo thiếu</Text>
            <Text>Hiển thị danh sách sinh viên báo thiếu hoạt động.</Text>
        </View>
    );
};

export default TroLySV_XemBaoThieu;
