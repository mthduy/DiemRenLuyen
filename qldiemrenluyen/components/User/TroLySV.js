import { View } from "react-native";
import { Text } from "react-native-paper";

const TroLySV = () => {
    return (
        <View>
            <Text>Trang dành cho Trợ lý Sinh viên</Text>
        </View>
    );
};

export default TroLySV;
