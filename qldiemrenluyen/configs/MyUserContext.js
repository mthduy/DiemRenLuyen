//Dùng để lưu thông tin ngữ cảnh toàn phần cho component sử dụng 
import { createContext } from "react";

export const MyUserContext= createContext(); 
export const MyDispatchContext= createContext(); 